# Manual PDF Workspace

This folder holds the content for `manual.pdf`.

This PDF explains how to use the framework.

Build it from the repository root with:

```bash
uv run python -m src.tools.build_docs --support-only
```

The finalised PDF is exported and placed in the repository root as `manual.pdf`.
