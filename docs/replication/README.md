# Replication PDF Workspace

This folder holds the content for `replication.pdf`.

This PDF explains how the reported results can be replicated, and should contain.

Build it from the repository root with:

```bash
uv run python -m src.tools.build_docs --support-only
```

The finalised PDF is exported and placed in the repository root as `replication.pdf`.
