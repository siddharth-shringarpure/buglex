# Requirements PDF Workspace

This folder holds the content for `requirements.pdf`.

This PDF describes the dependencies and versions required to run or compile the project.

Build it from the repository root with:

```bash
uv run python -m src.tools.build_docs --support-only
```

The finalised PDF is exported and placed in the repository root as `requirements.pdf`.
